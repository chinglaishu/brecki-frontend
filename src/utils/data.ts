import { User } from "../type/common";
import { StatusModalProps } from "../component/modal";

export const guest: User = {
  username: "guest",
  language: "en",
  isGuest: true,
  isLoading: true,
};
